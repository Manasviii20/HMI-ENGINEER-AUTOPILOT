# LLM configuration. Leave ANTHROPIC_API_KEY blank to run in full mock/demo
# mode -- the entire workflow (requirement -> plan -> generation -> validation
# -> self-correction) works without any API key.

LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=
MODEL=claude-sonnet-5
